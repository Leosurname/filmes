// Endereço do servidor que roda no computador de casa.
//
// Deixe vazio para a página falar com o próprio servidor que a serviu — é o que
// acontece quando você abre o app pela rede de casa.
//
// Preencha com o endereço do túnel quando estiver usando a página publicada no
// GitHub Pages. O endereço muda a cada vez que o túnel sobe, a não ser que você
// use um túnel nomeado (veja o README).
//
//   window.FILMES_API = "https://algum-nome-aleatorio.trycloudflare.com";
window.FILMES_API = "";
